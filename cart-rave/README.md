# Cart Rave (prototype)

Open `index.html` via a local server and drive the cart around the spinning record.

```bash
python -m http.server 5173
```

Then open `http://localhost:5173/`.

Controls: **WASD** or **arrow keys**. Space = **horn**.
