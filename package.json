{
  "name": "cart-rave",
  "private": true,
  "type": "module",
  "scriptsMeta": {
    "_doc": "Script guide — see docs/preview-dev.md for next-level branch workflow",
    "dev:next-level": "PRIMARY on next-level: Vite + preview PartyKit locally (no deploy)",
    "dev:party": "V2 server only (cart-rave-v2); separate from production cart-rave project",
    "dev:party:preview": "PartyKit preview worker only (port 1999); pair with npm run dev",
    "deploy:preview": "SECONDARY: public shareable preview URL only; prefer dev:next-level daily"
  },
  "scripts": {
    "dev": "vite",
    "dev:next-level": "node scripts/dev-next-level.mjs",
    "dev:party": "npx partykit dev --config partykit.v2.json -p 1999",
    "dev:party:preview": "npx partykit dev --config partykit.preview.json -p 1999",
    "build": "vite build",
    "preview": "vite preview",
    "build:party-static": "npm run build && node scripts/party-deploy.mjs",
    "deploy:party": "node scripts/party-deploy-guard.mjs && npx partykit deploy --config partykit.v2.json",
    "ship:party": "node scripts/party-deploy-guard.mjs && npx partykit deploy --config partykit.v2.json",
    "ship": "node scripts/party-deploy-guard.mjs && npm run build:party-static && npx partykit deploy --config partykit.v2.json",
    "deploy:preview": "npx partykit deploy --config partykit.preview.json",
    "compress:rave-gltf": "node scripts/compress-rave-gltf.mjs",
    "knip": "knip"
  },
  "devDependencies": {
    "knip": "^6.20.0",
    "partykit": "^0.0.115",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.19.3",
    "animejs": "^4.0.0",
    "howler": "^2.2.4",
    "nipplejs": "^1.0.4",
    "partysocket": "^1.1.16",
    "three": "^0.164.1",
    "tweakpane": "^4.0.5",
    "zustand": "^5.0.14"
  }
}
