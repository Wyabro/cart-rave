{
  "name": "cart-rave",
  "private": true,
  "scripts": {
    "dev": "npx partykit dev",
    "ship": "git push && npx partykit deploy"
  },
  "devDependencies": {
    "partykit": "^0.0.115"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
