# Cart Clash — Backlog (open work only)

**What is this?** Every known **open** item, deduplicated — grouped by discipline, prioritized.
**Why does it exist?** So open work lives in one place instead of scattered tables.
**Who should read it?** Whoever is picking the next piece of work.
**Related:** [STATUS.md](../STATUS.md) (declared phase + focus), [ROADMAP.md](./ROADMAP.md)
(phase definitions), [completed-work.md](./completed-work.md) (shipped),
[netcode-deep-dive.md](./netcode-deep-dive.md) (hazard writeups).

Priorities: **Critical** = blocks Version 2 · **High** = should land before V2 ·
**Medium** = V2-window polish · **Low** = post-launch / opportunistic.

Completed rows are **not** kept here — move them to [completed-work.md](./completed-work.md).

**Playtest console seed:** when a shipped change still needs a human check, put
`Owed: Wyatt playtest — ID — one-line check` in the Notes cell (or STATUS active-queue status).
`npm run dashboard` / `npm run playtest:console` rebuilds `.diag-captures/playtest-console.html`
from those phrases. Remove or rewrite to `Wyatt playtest PASS` when closed.

Do not re-add closed IDs (NET-1, NET-2, NET-MIG-3, NET-PRES-1, NET-SD-1, HOST-ROLE-1,
HOST-CAP-1, VFX-1, NET-CLK-*, NET-BUF-1, BOOT-PERF-1, COUNTDOWN-SYNC-1, HUD-FEED-1,
MENU-HINT-1, DIAG-DOC-1, ANLX-VIEW-1, ANLX-ATTRACT-1, ANLX-BULK-1, MP-FX-1, ARENA-COL-1,
SRV-TEST-1, HYGIENE-1, SKYBOX-1, SEC-BEACON-1, SEC-UNLOCK-1, SEC-ROUTE-1, SEC-TOKEN-1,
CARGO-RACE-1, CARGO-VIS-1, CARGO-WT-1, CARGO-HUD-1, CARGO-HUD-1a, SHEET-1, AI-DIFF-1,
HIT-FEEL-1, ARENA-BAL-1, INPUT-KB-1, SOLO-DIFF-1, LOD-UNCANNY-1, FX-TEXDISPOSE-1,
PIT-DEPTH-1, PIT-COL-INSET-1, SPAWN-BACKROOMS-1, CAM-OPEN-1, UNLOCK-ORDER-1,
CC-TOKEN-1, CC-STRIPE-1, CC-LABEL-1, CC-ICON-1, …)
without new evidence.

**Pre-ship 07-19 rows** tagged *(pre-ship 07-19)* are parked polish — pick up when Wyatt
names them; they do not auto-queue over STATUS.

**Pre-ship batch (07-31, Wyatt-named):** rows tagged `[pre-ship]` are **in scope before
ship** — not parked. Wyatt asked they all land pre-ship; priority still ranks order inside
that set.

**SHIP-1 tiers (07-20):** pre-ship ordering now lives in [SHIP-1.md](./SHIP-1.md).
Rows tagged `[SHIP-1 A–E]` are pre-ship, drained tier by tier; untagged rows default to
post-launch unless Wyatt pulls them forward.

---

## Engineering

| Pri | Item | Notes |
|-----|------|-------|
| High | ROUND-WEDGE-1 — host oscillates `podium ⇄ running` ~25×/s at round end (the wedged-round class, caught live) | **Filed 08-01 from cap-217** — the invariant watchdog fired in production, on Wyatt's own machine, and the capture is the evidence. **Elevate to Critical the moment it reproduces**; it is High only because frequency is unknown from n=1. **What the capture holds:** build `2e85f0b` (built 07-31T03:32:59Z), captured **07-31T05:09:05Z**, `is_host=1`, quickplay, `classicRecord`, RTX 4090, tier high, `?diag=1&room=quickplay`. The 512-event ring is **saturated at `EVENT_BUFFER_MAX`** (seq 769..1280) and holds **only two event types**: **171 `assert/phase-transition`, every one `podium → running`**, plus 341 `round/phase` (171 `running→podium` + 170 `podium→running`). It flips on a **~40 ms period** across `t 1283005→1288629` = **5624 ms minimum**; because seq starts at 769, roughly 768 earlier events were already evicted, so that window is the **tail of the storm, not its extent**. Final snapshot: `phase: podium`, `endReason: "timer"`, `winnerSlotIndex: 2`, `remainingMs: null`. **The assert is correct by design, not an over-strict table:** `invariants.js:24` names `podium→running` explicitly as the "wedged round" bug class, and `LEGAL_PHASE_TRANSITIONS.podium = ["countdown", "lobby"]` (`:33`). Two writers are fighting over `gameStore.roundPhase` at round end. **Player-visible**, which is why this outranks the pre-ship polish rows: a results screen re-entering RUNNING 25 times a second is a flickering podium, not a silent log entry. **Not the machine:** cap-215/216 (build `65dea12`, **same 4090, same quickplay, 7 `boot/play-entry` in one session**) carry **zero** asserts — this is not a standing condition of that box. **DO NOT blame ANLX-ATTRACT-1.** `2e85f0b` is the only `src/` commit in `65dea12..2e85f0b` and is therefore the obvious suspect, but it only **subscribes** to `gameStore` and emits analytics — it never writes `roundPhase`, so it cannot produce this. Attributing on adjacency is exactly run-4's "GC metronome" error that **HARNESS-NULL-1** exists to remember. It follows that the regression is either **older than `65dea12` and load/timing-dependent**, or in a path those two clean captures never exercised. **Next evidence, cheapest first:** (a) ✅ **RESOLVED 08-02 — both writers identified by reading the paths, not by adjacency.** Of the three candidates, **quickplay arena rotation is eliminated** and the other two are *both* implicated, fighting each other. **→ RUNNING is [netcode.js:2915](../../src/netcode.js:2915)**, the `MSG.round` applier, which is **not** host-gated; `netcode.js:2835` shows the rollback is *deliberate* — "Server rejected an optimistic host_round … Host may already be on podium while the room is still running — roll back" — with its own `onPodiumRejected` callback. **→ PODIUM is the host's own round-end in [gameFlow.js:149](../../src/gameFlow.js:149)**, gated only on `isHost && phase === "running"`, so the instant a rollback restores `running` the timer-expiry check re-fires `endRound()` on the very next frame, undamped. **The loop:** server MAX-rejects the host's round-end → keeps broadcasting `running` → applier rolls the host back → host immediately re-ends → rejected again, at broadcast rate. That predicts every measured property of cap-217 (host-only, `endReason: "timer"`, ~40 ms period) and explains why cap-215/216 were clean on the same 4090 — the rollback path only opens once the server starts rejecting, so it is not a standing condition of that box. Consistent with the card's own deduction that the cause predates `65dea12`. **Headline consequence: [invariants.js:24](../../src/utils/invariants.js:24) names `podium→running` as *the* wedged-round bug class while `netcode.js:2835` implements it on purpose — the table and the design contradict each other**, so all 171 asserts fired on intended behaviour at that site. Do **not** resolve this by adding `running` to `LEGAL_PHASE_TRANSITIONS.podium`: the oscillation is real and player-visible, and silencing the assert would hide it. The undamped re-entry is **Phase B**, which keeps its own ack. The four unguarded-looking `startRunningAt` sites were checked and cleared: `main.js:4616/4636/4645/4903` are all guarded by `phase === "countdown"`, and `:3041`/`:3126` are `testdrive`-only (cap-217 was quickplay); (b) the ring proves the oscillation but not its origin — the `assert` channel is in `AUTO_CAPTURE_CHANNELS` (`diagnostics.js:51`), so **up to 5 auto-capture bundles existed in memory that session** (`__ccDiag.captures()`, `AUTO_CAPTURE_MAX_PER_SESSION`) holding the pre-storm state and were never uploaded — ✅ **SHIPPED 08-02 `cc09985`**: auto-captures now POST to the same `/api/captures` endpoint F8 uses, so the next occurrence arrives via `npm run captures:pull` instead of dying with the tab. Gating stayed **structural** (auto-capture needs `apiRef` → `installDiagnostics()` → `main.js:5580` under `?diag=1`), pinned by a test so a future unconditional install cannot silently turn players into uploaders; volume bounds are unchanged (5/page-load, 5s debounce, error+assert only); (c) ⏸ **not attempted — deliberately, and not because it was forgotten.** It is listed as "if cheap" and it is not: the loop only opens once the *server* MAX-rejects, so a repro needs the host-hide condition plus a rejecting server, not merely a podium in quickplay. Now that (a) names both writers, a **unit** test against the two call paths is the cheaper next probe. **Instrumentation hazard, same card:** an assert storm **saturates the 512-event ring and evicts every other channel** — cap-217 carries no boot timeline, no perf, no `warmupSettle`, so the single most interesting session produced the least informative capture. ✅ **SHIPPED 08-02 `8063b3e`**: eviction now drops the oldest event from whichever channel occupies the **most** slots, so a flooding channel eats its own evictions and can never push a quiet channel below parity; degrades to FIFO when the storm is the only channel. Chose parity over a per-channel floor constant because the buffer is channel-agnostic and a fixed table would silently stop covering new channels. Dedupe was considered and **not** taken (Wyatt's call — floor is enough). **Does not re-open COUNTDOWN-QUICKPLAY-1** (a different phase edge — countdown entry, not podium exit) or the reverted host-countdown gate (`c8df8fd`). |
| ✅ | SHOOT-ANIM-1 — `shoot-gpu` captures freeze **all** per-frame level animation | **CLOSED 08-02** — fixed in `6b27283`, `--t` flag in `79a9caf`. **The root cause was one layer up from this card's own diagnosis, and was never capture-specific.** Not LOD, and not `settle()`: [menuAttract.js](../../src/ui/menuAttract.js) runs its own rAF loop that **rendered without ever updating**, while the game loop that owns `levelUpdate` bails in `shouldSkipTiming` whenever `menuVisible` is true — which the harness deliberately keeps true. The attract loop is therefore the *only* loop running during a capture. **The same was true for real players: the live main-menu arena was frozen for everyone** — water static, turbines stopped, beacons dead — so this was a player-facing bug, not only a QA one. **Fixed via the attract loop's new `onAnimationTick` dep, NOT via `settle()` — this card's own option (a), now superseded:** `settle()` only awaits rAF and never renders, so driving animation there lands a frame stale. The tick sits *below* the throttle gate and *before* the render, pairing update→render in one frame exactly as the game loop orders them; `updateLevelLod` keeps local wall time, since a small pinned `t` would park its module-global 250 ms latch in the future. Attract cost now times update+render together, so the auto-quality watchdog can actually see the added CPU. **Verified:** gate-beacon opacity went 0/23 → 11/23 frame-to-frame changes free-running (the attract loop throttles to ~30 fps, so ~every other rAF tick renders); `?t=1000` holds it at exactly `0.6066653819`, matching `0.45 + sin(1000×0.0009)×0.2` analytically; the live menu was confirmed animating on the ordinary no-harness path; zero console errors across 8 captures on real hardware (RTX 4090, not SwiftShader). **Bonus finding — a documented attribution was wrong.** [visual-qa.md](../guides/visual-qa.md) blamed Classic's 15–30% capture-to-capture variance on *crowd animation*, but the crowd was frozen in every capture (that is this very card) and is frozen still. Measured with animation **pinned**, so animation cannot contribute: Classic's null floor is **15.89%**, Sundial's is **~1.2%** (1.22% and 1.21% on two independent pairs). The real cause is **unseeded construction randomness** — `Math.random()` at build time, 56 call sites in `zanzibarPlatform.js` alone, including procedural texture painting, so every page load paints a different arena. Three 08-01 findings had their "evidence" sitting in that variance under the wrong name. Guide corrected; null floors and the two-phase `--t` recipe documented there. Doubles as the null-control arm **HARNESS-NULL-1** asks for, on this one rig. **Deferred → SHOOT-ANIM-2.** |
| ✅ | SHOOT-ANIM-2 — rave dressing is still frozen in captures | **CLOSED 08-02** — fixed in `33f0d15`. Body extracted to `tickRaveDressing()` + a shared `raveDressingWanted()` gate, called from the game loop **and** the attract loop; the gates that legitimately differ (frame budget, clock source) stay at the two call sites, so there is one definition and no drift. **Deliberately no frame budget on the attract path:** `frameBudgetAllow` **fails closed** without a preceding `beginFrameBudget` (stale `frameStartMs` → negative remaining), and its `allowCache` is only cleared by `beginFrameBudget`, so the first `false` would **latch that bucket permanently**; calling `beginFrameBudget` from attract instead would put two loops on one set of module globals, and the budget exists to protect host physics, none of which runs at the menu. Weak machines are covered here by the attract cost feed → auto-quality, which steps the tier down and flips `crowdAnimate` off. **Two of the five items this card named were filed on a wrong premise, corrected rather than quietly dropped.** (1) **Lasers and billboard were absent, not frozen** — `finalizeArenaShellForMenu` builds the shell with `includeJuice: false` ([main.js:2615](../../src/main.js:2615)) and the DEV log says "sky + stadium, no lasers" outright, and captures boot through that same menu path, so they were never in a capture to freeze. Left absent by ack; the `raveJuiceInitialized` sub-gate is kept so they animate on their own if the juice is ever built there. (2) **`fxPass.uTime` was never a capture bug — it is a live one**, split out as **FX-TIME-1**. Scope is **Classic Record only** (`levelUsesRaveExtras` is `id === "classicRecord"`). The **record spin** stays game-loop-only on purpose: it is `rotation.y += … * dt`, a delta accumulator coupled to `applySlowMoToDt`, so it cannot pin under `?t=` and would fight slow-mo if converted. **Verified** (dev server, RTX 4090): stage spots + crowd bulbs **0 → 11/23** frame-to-frame changes free-running, and **11/23 on the ordinary no-harness menu path**; **null control at `?preset=low` reports 0/23 on all ten objects while still finding them**, so the probe is provably not blind — a second null-control arm for **HARNESS-NULL-1**. Pinned `--t 500` vs `--t 6783` (6283 ms = exact antiphase for `sin(nowSec×0.5)` **and** 3 half-periods of `sin(nowSec×1.5)`) reads **36.24%** against a **14.51%** same-phase floor, and the amplified diff is decisive where the number alone would not be: searchlight shafts, stage columns and deck rings appear **only** in the signal, while the null carries crowd speckle alone — i.e. unseeded construction randomness, consistent with the ~15.9% Classic floor measured in SHOOT-ANIM-1. **Owed: Wyatt manual check** — sit at the Classic menu, then enter a real round and confirm the crowd still animates *in play* (proves the attract path left no `allowCache` / `frameStartMs` residue the game loop inherits; a headless capture cannot reach a live round). |
| Medium | FX-TIME-1 — the whole VHS/arcade layer has been frozen at `uTime = 0` in live gameplay | **Filed 08-02**, found while scoping SHOOT-ANIM-2 and split out because it is **not** a capture bug — it affects every player, every frame, and always has. **`fxTimer.update()` is never called anywhere in `src/`.** The only two references to `fxTimer` are its construction ([main.js:1670](../../src/main.js:1670)) and the read ([main.js:5280](../../src/main.js:5280)); `THREE.Timer._elapsed` only advances inside `Timer.update()`, so `getElapsed()` returns `0` on every frame and `fxPass.uniforms.uTime` is pinned at zero. **What is currently dead** ([scene.js:511-596](../../src/scene.js:511)): VHS line noise (`:511`), the tracking-bar phase (`:514`), the tape-tear clock, position and width (`:523-526`), the tear jitter (`:531`), the chromatic-aberration wobble (`:536`), film grain (`:552`), scanline noise (`:560-561`), and the sudden-death pulse + rays (`:594-596`). Grain and scanlines currently render as a **fixed dither pattern** rather than moving noise, and tape tears never fire at all. **Fix is one line** (`fxTimer.update(now)` in the frame loop) **but the consequence is a real look change**, switching on animated grain, moving tracking bars, tears and the SD pulse for the first time — so it needs a look sign-off and pairs naturally with the open **Bloom look sign-off** row, not with a harness card. Judge it on Storerooms (where the VHS treatment reads strongest) and on the SD pulse. Ordinary captures cannot evidence it either way until it is driven — and note a driven `uTime` would then be **wall-clock**, so it would need folding into the `?t=` pinning story before any pinned Classic/Storerooms A/B stays reproducible. |
| Medium | SPAWN-SUNDIAL-1 — Sundial platform-leg colliders `[pre-ship]` | **Half closed 08-02.** The spawn inset shipped in `e64f1a3` via `booth.gapDistanceByLevel` — Sundial's booths and spawn ring both moved +0.75m outward with no level-file edit. **Still owed:** colliders on the platform legs. `buildZanzibarBooths` ([zanzibarPlatform.js:2508](../../src/levels/zanzibarPlatform.js:2508)) builds four 11m legs plus cross-braces per booth as **visual meshes only** — the deck cuboid at `:2579` is the sole `createCollider` in the builder, so carts ghost straight through the supports. Deferred because `zanzibarPlatform.js` was owned by the parallel ART-PASS-SUNDIAL-1 session; pick up once that card closes. |
| Low | SPINDLE-COLOR-DEAD-1 — Classic still declares and returns a colour pair nobody reads | **Filed 08-03**, found while closing Sundial audit item 28 and deliberately left out of that commit (Wyatt's instruction: do not expand into Classic). Sundial's spindle "colour contract" turned out to be dead — `main.js` destructured `spindleLightColorPink`/`Cyan` into locals and never read them — so `60486c9` moved the cycle into Sundial's own `update()` and deleted the locals, Sundial's returns and its typedef. **Classic was not touched and still declares the pair at [arena.js:1672](../../src/arena.js:1672) and returns it at [:2786](../../src/arena.js:2786),** while its live reactive path uses `reactive.accentColor` ([:2753](../../src/arena.js:2753)) instead. So the pair looks dead there too. `backroomsSupermarket` keeps its own pair on purpose, labelled inert, for API shape. **Check before deleting** whether anything reads Classic's pair through the shared level-result shape (`levels/index.js` types them optional, so the type system will not tell you). Cosmetic only — no player-visible effect either way. |
| High | PERF-PASS-1 — low-end performance is worse across the board | **Filed 08-03 from Wyatt's playtest note**, alongside ATTRACT-JANK-1 below. Report is a felt regression on weak hardware, not a number, so this card is **measure-first and nothing else until there is a delta**: the standing rule from run-4's "GC metronome" error (**HARNESS-NULL-1**) is that adjacency is not attribution. **Known floor to measure against:** run 5 put the Intel UHD iGPU at LOW at **54% of frames over 33 ms** — that is the pre-existing smoothness floor, so "bad on the Intel box" is only evidence if it is *worse than that*. **Method:** bisect recent batches on the Intel box, **prod not dev** — dev-only probes (`__cartRavePerf.scene`, `import("/src/…")`) lie in prod, and dev-server timing is not the shipped bundle. Candidate surface added since the last known-good measurement is large (Sundial god rays + hologram, the three LOAD-POSTER-1 loading scenes, octagon/holo work, FX layers), which is exactly why a bisect beats guessing. **Do not** start tuning tier knobs before a measurement — `PERF-TIER-1` and `PERF-WATCH-1` are the levers *after* attribution, not instead of it. Related: WARM-SOLO-1 (weak-GPU first-draw stalls), PROBE-WARM-RT-1. |
| High | ATTRACT-JANK-1 — attract-mode animation reads as jank on weak machines | **Filed 08-03 from Wyatt's playtest note**, split from PERF-PASS-1 because it has a specific and recent suspect worth checking before any general sweep. **The attract loop only started animating on 08-02.** SHOOT-ANIM-1 (`6b27283`) found `menuAttract.js` was rendering without ever updating — the live main-menu arena was frozen for every player — and added the `onAnimationTick` dep; SHOOT-ANIM-2 (`33f0d15`) then put rave dressing (stage spots, crowd bulbs) on that same loop. So the menu went from static to fully animated in one day, immediately before this report. **That makes it the first thing to measure, not the confirmed cause** — same discipline as PERF-PASS-1. **Two specifics that matter if it is the loop:** (1) the attract path deliberately runs with **no frame budget** (`frameBudgetAllow` fails closed without `beginFrameBudget` and would latch a bucket off permanently), so its per-frame cost is unbounded by design and weak machines rely entirely on the auto-quality watchdog stepping the tier down; (2) attract render cost and in-game frame delta are judged against **one shared 20.5 ms bar** (see PERF-WATCH-1), and demotion is irreversible per session — so a heavy menu can demote the tier before the player ever enters a round. Check whether the watchdog is firing at the menu on the Intel box. |
| Low | COUNTDOWN-QUICKPLAY-1 — empty quickplay countdown connect-wait edge case | In empty quickplay games, countdown either waits for player connection before starting or skips part of it. Documented from F8 captures (184–196); parked in backlog per Wyatt (07-22). |
| 🟡 Partial | NET-PERF-1 — reconcile rewind-replay cost | Caps shipped; residual if retest still rubber-bands. |
| Low | NET-PERF-3 — p2p per-message buffer copy | Only batch if F8 shows alloc pressure after NET-PERF-1. |
| Medium | Host-reload mid-round live confirm | Automated half: netharness `hostReload` (A6b). Optional: one live HOST-tab reload smoke for feel. |
| Medium | Customize screen performance pass *(pre-ship 07-19)* | Measure before tuning. |
| Low | Countdown timer survives menu return *(pre-ship 07-19)* | Stale countdown UI on main menu. |
| Medium | NET-SIM-1 — socket-lifecycle netharness scenarios `[SHIP-1 A6]` | A6a (party-do silent-reap + ghost 4010) + A6b (netharness `hostReload`) landed; extend as needed. P2P zombie + reconnect cooldown already unit-covered. |
| Medium | HARNESS-FRIENDS-1 — netharness covers quickplay-shaped joins only; friends ready-up/lobby/rematch is manual | **Filed 08-01** (audit triage). Automated MP is quickplay-path / `?room=` join (`docs/guides/netcode-harness.md` coverage gap). Friends private-room ready-up, CHECKOUT LINE lobby, and rematch stay Wyatt/manual. One scenario later: friends room → ready-up → countdown → rematch. Tooling only — does not replace FIGHT-VERIFY-1 Wyatt half. Extends NET-SIM-1 lane. |
| Medium | WARM-SOLO-1 — solo post-`carts-ready` stall (WARM-IGPU-1 residual) | Laptop A cap-206 (**solo**) took a 6.4s longtask ~1.9s after `carts-ready`, inside the countdown. WARM-IGPU-1's Lever A does **not** cover it: arena rotation is quickplay-only, and solo's flyover warm already runs inside `ensureSessionCartsReady`. Proxy evidence says the residual is driver-side first-draw cost (a 13.1s menu-warm frame carried only 235ms of attributed span time), so raising budgets will not help. Candidate mechanism worth checking first: scene content added *after* the warm pass (CSS2D nametags, cargo bays — CARGO-RACE-1's self-heal adds 18–30 meshes per cart, announcer/VFX) introduces new materials whose programs link at the first live countdown draw. **Work only on real telemetry** (`warmupSettle` / longframe spans from a weak-GPU playtester), never on speculation — no iGPU hardware available to reproduce. |
| Medium | GIT-INDEX-1 — concurrent agent sessions share one git index, so `git commit` ships work the committing session never staged | **Filed 08-01 after it fired TWICE in one day, in both directions.** (1) An agent's uncommitted `docs/ARCHITECTURE.json` was swept into another session's `64b49d2` ("chore(claude): enforce three AGENTS.md rules as Claude Code hooks"), leaving HEAD briefly carrying a manifest that described `CC-COHERE-1` / `CC-TOKEN-1` / `CC-STRIPE-1` while the committed BACKLOG had none of those rows. (2) The reverse: `.agents/skills/brainstorming/SKILL.md` plus 9 lines of `docs/STATUS.md`, staged-but-uncommitted by the other session, rode along inside `35859cc` (CC-COHERE-1) — then got pushed with it. **Mechanism:** one working tree = one `.git/index`. `git add <paths>` is additive and well-behaved, but **`git commit` with no pathspec commits the ENTIRE index**, including whatever another session staged seconds earlier. Careful staging by one session does not protect it from another session's commit. **Why the existing guard misses it:** `.claude/hooks/guard-git-add.mjs` (`64b49d2`) enforces the AGENTS.md "never `git add -A`" rule — but **neither incident involved `git add -A`**. Both used correct, narrow `git add <explicit paths>`. The leak is one step later, at `git commit`, which the guard does not inspect. **The damage so far was only attribution — that is luck, not safety.** The real failure mode is a session committing another session's *half-finished* edit: it ships under the committing session's green `qa` run, having never been gated on its own, and the authoring session then sees a clean tree and believes it already landed. **Levers, cheapest first:** (a) make `git commit -- <paths>` the convention — with a pathspec, git commits those paths from the working tree and ignores the rest of the index; (b) extend `guard-git-add.mjs` (or add a sibling) to intercept pathspec-less `git commit` and refuse/warn when the index contains paths this session did not stage — the hook already has the interception point, it just watches the wrong verb; (c) structural fix — one git worktree per session, which gives each its own index. The repo **already uses** `.claude/worktrees/` for some agent work, so this is an existing pattern to extend rather than a new mechanism. Related in character to **HARNESS-NULL-1**: a process that cannot demonstrate it did what it claims. |
| Low | DIAG-UPLOAD-GEN-1 — the auto-capture upload continuation is not generation-guarded, so a torn-down hub can still file a bundle | **Filed 08-02**, split out of DIAG-FLAKE-2 rather than folded in, because it is a **production behaviour change** and that card was test determinism. **Mechanism:** `scheduleAutoCapture`'s timer callback checks `hubGeneration` before assembling, but `uploadAutoCapture` never re-checks it anywhere in its continuation. A hub torn down or re-installed *while the upload is in flight* still POSTs to `/api/captures`, so a bundle lands labelled against a session that no longer exists. Same family as DIAG-FLAKE-1, one layer further out: that card fixed the deferred *assembly*, this is the deferred *send*. **Why Low:** the window is the length of one upload and the bundle content is still truthful — only its session attribution is stale, so it cannot corrupt a live session, only add a confusing row to a pull. **Fix:** capture `hubGeneration` in `uploadAutoCapture` and drop the post if it moved — the same three-line shape `be350b4` used. `tests/diagnostics.test.js` already has the drain (`__drainAutoCapturesForTest`) needed to test it deterministically: schedule, let the chain start, re-install, assert nothing posts. **Do not** fix it by cancelling in-flight uploads on teardown — fire-and-forget on every axis is deliberate (evidence collection must never block the app), and an abort path is a bigger change than the bug. |
| ✅ | HOOK-INDEX-1 — pathspec commit left stale staged generated docs | **CLOSED 08-03** — post-commit runs clear-stale-generated-index.mjs first (before dashboard): if index blob ≠ HEAD blob for docs/BRIEFING.md / docs/ARCHITECTURE.json, git restore --staged. Fail-open; not coupled to HTML refresh. Tests: 	ests/hookIndexPostCommit.test.js. |
| ✅ | STOP-DIRT-1 — Stop guard blocked on other session dirt | **CLOSED** (code 08-01; BACKLOG row retired 08-03) — guard-stop-drift.mjs scopes dirt to session-owned paths via elevantDirty + 	rack-session-writes. Open Medium row was stale thrash bait. See completed-work. |
| Medium | HARNESS-NULL-1 — no measurement rig has a null-control arm | Process/tooling, not behavior. Every A/B number this repo produces is currently unfalsifiable: no rig can demonstrate it reports ~0 when nothing changed, so warm-up order, run ordering and accumulated state are indistinguishable from a real effect. We have already been burned — run-4's "GC metronome" attribution was wrong. The pattern: add a `--null` mode that runs **both** arms with the variable disabled, making the two runs byte-for-byte the same experiment; the measured delta must then be ~0, and anything else means the rig is biased and every number it prints in normal mode is contaminated rather than caused. Candidates in rough order of value: `perf-profile.mjs`, `battery.mjs` timing steps, `gameharness.mjs --scenario soak`. Prerequisite for trusting PROBE-WARM-RT-1's program-count deltas and for any future unattended tuning loop (an overnight loop compounds instrument error across every run). Source: `ryancampbell/kart-royale` `tools/drift-bench.mjs --null`; loop framing from `karpathy/autoresearch` `program.md`. |
| Medium | PROBE-WARM-RT-1 — VFX program anchors may be holding the wrong program key | Instrument-first; **no behavior claim until measured**. `outputColorSpace` and `toneMapping` are both pushed into three's program cache key (`getProgramCacheKeyParameters`) and both switch on `renderer.getRenderTarget() === null` — `outputColorSpace` unconditionally, not just for `toneMapped` materials. `compileAsync` (`main.js:2664`) binds no RT, so the anchors compile the **default-framebuffer** variant; the `composer.render()` prime (`:2679`) builds correct RT-variant keys only for what it actually *draws*, and the anchors are `visible=false` at `y=-500` (`koHitmarkerFx.js:259`, `cartShatter.js:1062` — both comments say "render skips them"). If that holds, the anchors' stated job (next KO is a cache hit) is defeated and the first shatter/KO/water/ram spawn links synchronously mid-round. **Measure first:** `renderer.info.programs.length` across the first KO. Fix only if it climbs — bind any non-XR RT around the anchor compile (1×1 scratch is enough; only `=== null` is tested). Making the anchors visible does *not* work: they are off-camera and cull. Pairs with WARM-SOLO-1 — same symptom class, different mechanism (that one is new content added after the warm; this one is the right content under the wrong key). Source: `ryancampbell/kart-royale` `src/core/Prewarm.ts`. **W0.1 evidence 08-03 (cap-229 @ c418bd9):** after Cart Rave play-shader settled at materials=497, a mid-round `warmupCompile` at materials=505 + **6.5 s** longtask/`warmupSettle` poll fired ~8 s into running — late program link signature consistent with RT-variant miss. Still measure-first; not in PLAYTEST-BATCH-0803 scope. |
| Medium | NET-RING-1 — decode-ring reject counters (review C-03) | Instrument-first. Rejects (dup/ooo seq etc.) burn ring slots AFTER decode; `netStateBuffer` retains ring-owned cart arrays by reference (`netcode.js:1422→1434`); true margin = 96−rejects, not 32, and only bites when consumption stalls. Count rejects-since-oldest-buffered; the copy-into-pooled-record fix only if counters show real traffic. |
| Medium | PERF-WATCH-1 — auto-quality step-up path | Watchdog demotion is irreversible per session (no step-up anywhere; DEV-only warn; 2 tier steps + 2 renderScale steps; attract render-cost and game frame-delta both judged against one 20.5ms bar). Decide after WARM-IGPU-1 P0b telemetry shows how often it bites. |
| Medium | PERF-TIER-1 — `high-lite` tier rung | `DISCRETE_GPU_RE` puts a 1660 Ti in the same discrete→High bucket as a 4090; High→Medium cuts 4 knobs at once (DPR 2→1.25, reflector off, crowd, lasers). Blocked on HYGIENE-1's `--dpr` profiling — tier table may be tuned against an inverted ranking (512px reflector is DPR-invariant; full-screen cost ×4 at DPR 2). |
| Medium | `structuredClone` → flat serializer in `party/index.ts` | Only after profiling shows it matters. |
| Medium | Persistent leaderboard / player stats `[SHIP-1 D2]` | Needs TRUST-1. |
| Low | Quickplay rotation live 2-browser check | Reconfirm after **QP-ORDER-1** (sequential rotation). Live multi-client smoke only. |

## Art

| Pri | Item | Notes |
|-----|------|-------|
| ✅ | SUNDIAL-DECK-DETAIL-1 — deck surface density (Sundial audit item 34) | **CLOSED 08-03 BY MEASUREMENT, not by building it.** The audit's largest single item — tiling detail layer, salt-crust bloom, spray staining, sun bleach, rubber skid arcs, braking chevrons, re-authored plate seams, re-blitted decals. A relief-only probe was built and then removed in `586330b`: a tiling steel-grain normal at 1 m repeat on a transparent overlay (the [arena.js:1719](../../src/arena.js:1719) vinyl-detail-ring pattern), `normalScale` deliberately over-strong at 1.4 so a null result could only mean "no". **Toggling ONLY `normalScale` (0.0 vs 1.4, tint identical in both, same camera, pinned phase): deck median 2.43 → 2.43 (0%), p75 5.06 → 5.06 (0%), whole frame 0.86% — BELOW Sundial's ~1.2% construction-noise floor.** Relief contributes nothing here. **The reason also explains Wave 4's paint result rather than sitting beside it:** a normal map modulates SHADING, and this plate is lit to a median of 2.4/255 — modulating almost no light still gives almost no light, which is why paint measured 16.4 against emissive's 153. **A trap worth knowing:** the overlay moved 8.31% of the frame against the previous commit, which reads like a win; it was the overlay's own dark albedo tinting the plate 8% DARKER. Judge this class of change by isolating the channel, never by whole-frame delta. **Do not re-open** without new evidence about the deck's *lighting* — what moves this surface is light (item 16's god rays took the median up 82%) and emissive, not surface authoring. Contrast SUNDIAL-RAMP-1 below, where the same probe said yes. |
| Low | SUNDIAL-LOW-WATER-1 — Low still skips the ocean normal map | **Filed 08-03**, the one residual after Sundial audit item 36 was otherwise closed. `waterNormalTex` is still built inside `if (!lowQ)` ([zanzibarPlatform.js:1427](../../src/levels/zanzibarPlatform.js:1427)), so Low's ocean has no ripple normal — the audit asked to keep it ("a 256 px canvas built once; one extra texture fetch, not a pass"). Everything else item 36 named is now on Low and was verified in source 08-03: foam ring (built on every tier with an explicit D-SUNDIAL-OQ6 comment), sun-path glint (ungated), 2 of 3 god-ray shafts (`rakeCount = lowQ ? 2 : 3`), a Low-specific ocean roughness of 0.5, and the reduced hologram — core + one glyph band + one ring — shipped in Wave 5 `0428f17`. Filed rather than built because Wave 6's close-out lever was scoped as verification only. Cheap; check the cost on the Intel UHD box, which is the min-spec smoothness floor. |
| Low | CART-FORK-SWIVEL-1 — the un-painted fork piece still does not steer with its caster | **Filed 08-02**, split out of CART-FORK-1 rather than folded in, because it is behaviour and that card was a one-line material fix. `tripo_part_23` is now correctly roled `fork` (`cartRaveGltf.js`), but it appears in **no** entry of `RAVE_GLTF_V4_FORK_GROUPS`, so it stays static while the caster it belongs to swivels. Its mirror twin `tripo_part_22` **is** in the BR group's `forkParts` and does swivel, which is the asymmetry to close. By position (`[-0.148, 0.197, +0.236]`, from the master GLB accessor bounds) it belongs to the **BL** group, whose `forkParts` is currently `["tripo_part_5", "tripo_part_21"]`. Cheap now that the role is right — `buildRaveGltfCaster` only accepts meshes already roled `fork` (`cartRaveGltf.js` ~:1963), so before CART-FORK-1 it could not have been added at all. **Check before shipping:** whether the piece is rigid with the fork legs or is authored at the connector, since `captureRaveGltfCasterRestTransforms` bakes rest transforms from whatever is in the list; and confirm `classifyCartrave4AnimRole` still lets it animate (centreY 0.197 and maxDim 0.056 are both well under the 0.38 / 0.48 static cutoffs, so it should). Verify by steering in a live match — the attract capture path does not exercise caster swivel. |
| High | Bloom look sign-off (Classic/Sundial) `[SHIP-1 E2]` | Art half of closed VFX-1. Success criteria now come from **ART-EXPO-1 / ART-FILTER-1**, not "dark arenas + punchy neon" — see [art-direction.md](../reference/art-direction.md). |
| Medium | Wilting-groceries Defeat screen reads as "confetti / something good" `[SHIP-1 E2]` | Needs art-direction call before code — see [art-direction.md](../reference/art-direction.md). Pairs with **PRE-PODIUM-1** (effect must fire first; then look). |
| High | CART-MODEL-1 — new cart basket/model `[SHIP-1 C1]` | Wyatt-led Blender work completing the prototype-era cart design. Author against the **cart material contract** in [art-direction.md](../reference/art-direction.md) (required maps per slot, 2nd UV channel, wear language) — **not blocked by ART-MAT-1**. While in Blender: clean body UVs / 2nd UV channel — unblocks patterns ([cart-pattern-reuv.md](../guides/cart-pattern-reuv.md)); fold **CART-FORK-1** if convenient. |
| High | ART-MAT-1 — authored maps on the carts | **Scope corrected 08-01:** all three arenas already author their hero surfaces (Classic vinyl + pit wall carry albedo/normal/roughness; Sundial's deck authors seams, rust and traffic scuffs; Storerooms has ~9 builders). The **carts are the only Rule 1 failure** — zero maps on any slot ([cart.js:118](../../src/cart.js:118)). Largely absorbed by **CART-MODEL-1**, which authors against the material contract in [art-direction.md](../reference/art-direction.md). |
| High | ART-PASS-1 — per-level art pass audit `[pre-ship]` | One level at a time: which authored surfaces read thin, which elements are bare filler beside well-authored neighbours, what is identity and must not change. **Not a texturing sweep** — the arenas already have maps. All three arenas audited 08-01 (6 element groups each, adversarially verified). **ART-PASS-CLASSIC-1** is the Cart Rave execution card. |
| High | ART-PASS-CLASSIC-1 — Cart Rave art pass `[pre-ship]` | One commit + one real-GPU capture per lever. **L1 ✅ `316c74f`** pit-wall texel anisotropy — `repeat.set(16,200)` → `(32,69)` squares the tile, killing a measured 5.8× (rivets rendered as ~5.8:1 ovals). **L2 ✅ `beebe81`** the CART RAVE label had never rendered — wordmark, divider, hole cut and grooves 1–3 of 7 all sat inside the `RingGeometry(3.7, 7.0)` inner edge at canvas r=135.3px; re-authored as curved arc text, geometry constants now shared by canvas and mesh. **L3 ✅ `d59fd92`** records on the turntables + platter wear map; platters **ruled hero, not dressing** (allowlist updated in the same commit). **L4 ❌ DROPPED** → see CLAD-REPEAT-1. **L5 ✅ `5fc1c1e`** (row corrected 08-02 — it read "remaining" after the commit landed) — record-body `ExtrudeGeometry` UVs were in metres and the vinyl maps default to ClampToEdge (`arena.js:439`/`298`); fixed with a custom UVGenerator normalising to 0..1, but shipped as **correctness only, no visual delta**: it is rim-band only, since two `RingGeometry` meshes with correct 0..1 UVs cover the body's top face at every tier. **Card COMPLETE.** |
| Low | CLAD-REPEAT-1 — stand cladding shares one repeat across three deck radii | **Was ART-PASS-CLASSIC-1 L4; dropped 08-01 and demoted, because the surface is barely visible.** The defect is real and measured: one `panelTex.repeat.set(24, 3)` (`effects.js:1441`) feeds one shared `cladMat` across three decks (`effects.js:1454`) whose r1 = 73/100/124 m and wallH = 12.2/10.6/9.8 m, so the authored 2:1 cart-silhouette motif renders **2.09×0.22 m on deck 0 and 3.55×0.18 m on deck 2** — 4.7× and 9.9× distorted, and inconsistent between rings. **Why it was dropped:** cladding sits at `deck.r1 + 0.55`, directly behind seating spanning r0→r1, so from every in-arena viewpoint tried the crowd and seats occlude it; a before/after GPU capture showed *zero* delta on the cladding itself (the 31% pixel diff was animated-crowd noise). A surface you cannot see does not earn a mid-table slot. Fix if ever picked up: per-deck material+texture clone like the seat loop (`effects.js:990`), `U = round(circumference / wallH)`, `V = 1` — the largest square tile each wall fits. |
| Low | LOD-PITRING-1 — the pit-ring dressing's cull radius is arguably inverted | **Filed 08-02**, split out of audit item 3. `registerLevelLodNode(pitDressing.group, { far: 48 })` — but `buildPitRingDressing` lays its silhouettes on a band at `OUT = 45.5` m radius ([backroomsSupermarket.js:2205](../../src/levels/backroomsSupermarket.js:2205)) around a group at the origin. So the ring is visible while the camera is near arena centre (far from the dressing) and **hides once the camera passes 48 m from centre — i.e. exactly when it gets close to part of the ring.** Distance-to-centre is the wrong metric for a ring centred on that point; the pit dressing either wants per-cluster nodes (one per side) or no LOD at all, since it is a handful of merged silhouettes. `doorways` (far 55, on the walls at 56) has the same shape and should be judged in the same pass. Lower priority than LOD-UNCANNY-1 because this is background dressing beyond the kill edge, not a fall marking. |
| Low | SHELF-RAIL-1 — the booth rails are the shiniest thing in a dead room | **Filed 08-02**, split out of audit item 4 rather than folded in as a silent ride-along. `railMat` ([backroomsSupermarket.js:2948](../../src/levels/backroomsSupermarket.js:2948)) is the **booth** rails, a different surface from the shelf steel, and separately the lowest-roughness / highest-metalness pair in the file — so under the RoomEnvironment PMREM it reads as polished chrome in a room where nothing else is polished. Item 4's new `buildShelfSteelTexture()` is a natural donor, but the rails also want their own roughness call, and doing both in one commit would have made the item-4 capture ambiguous. Also parked here: **per-bay board segmentation** (break each 114 m shelf board into bays with a 4 cm gap so the run reads as bolted sections) — a geometry/merge change, not a material one, and the audit lists it under the same item. |
| Medium | SKYBOX-DIR-1 — does a space skybox serve an underground-warehouse-party mood? | **Direction question, not a defect.** The 08-01 audit measured the visible sky as a ~0–5° sliver and found the arena's worst-authored props (3580 stars, gas giant, 2 UFOs) are the ones sitting in it — turned on for the first time by SKYBOX-1 (07-30, +54 draw calls). Either commit to it and author it properly, or cut it and spend the budget on the room. Wyatt's call; do not resolve inside an art-pass lever. Split out of ART-PASS-CLASSIC-1. |
| Medium | ART-FILTER-1 — arcade pass gated to The Storerooms | The CRT layer (aberration/scanlines/vignette) is written once in `createComposer` from global config, so every level inherits it. Add a per-level gate mirroring the VHS gate (`main.js` ~2448); preserve the impact-pulse base capture (`main.js` ~1110 / `frameVisuals.js` ~630). Before/after `npm run shoot` on all three arenas. Satisfies Rule 2. |
| Medium | ART-EXPO-1 — retire the global exposure lock | Replace the single `toneMappingExposure: 0.4` with a per-arena budget alongside `arenaExposureMul`. Captures the Rule 3 luma-floor baselines into [art-direction.md](../reference/art-direction.md). Fresh sign-off with before/after shots. |
| Low | ART-PALETTE-1 — reconcile 3D and 2D neon | 3D is frozen on pure `CART_COLORS` (`0xff00ff`); 2D banned those hexes as off-brand and uses `#ff2bd6`. **The only card permitted to unfreeze the AGENTS.md invariant.** |
| Medium | Pattern customize UI `[SHIP-1 C3]` | Unblocked by CART-MODEL-1's re-UV. |
| Low | Sunglasses finish materials broken `[SHIP-1 E2]` | |
| Low | Asset filename rebrand (`cart-rave-base*.glb` etc.) | Deliberate asset pass — [brand.md](../brand.md). |

## Audio

| Pri | Item | Notes |
|-----|------|-------|
| High | MENU-MUSIC-VOL-1 — menu music louder than set volume after loop restart `[pre-ship]` | After the menu song restarts/loops, gain sometimes exceeds the player's set music volume. Suspect Howler loop / re-play path ignoring or re-applying volume wrong. |
| Medium | HIT-SFX-VAR-1 — more cart hit SFX variety `[pre-ship]` | Current hit clips are too repetitive. **Blocked on Wyatt providing new sound clips**; then wire into the hit pool (random / cooldown / velocity tiers — pick cheapest that reads varied). |
| Medium | Announcer re-records (Wyatt) `[SHIP-1 E3]` | Shorter directive takes + odd lines. Pipeline drop-in. |
| Medium | Sudden Death music low-pass `[SHIP-1 E3]` | Audio-graph surgery (shared Howler bus). |
| Low | Deeper Howler upgrade `[SHIP-1 E3]` | Spatial, pooling, volume groups. |

## Design / Gameplay

| Pri | Item | Notes |
|-----|------|-------|
| Medium | Taste-tuning follow-ups from Pass 4 | Only reopen with playtest evidence (D-GP4-1). |
| Medium | Clutch slow-mo (Pass 5 deferral) | Taste-gated. |
| Low | Turntable swirl force revive | Scoped prototype via DIR-1 — taste-gated. |
| Low | KO "doomed" presentational cue | Idea stage. |
| Low | Death-cam "follow killer" revisit | Previously reverted. |
| Low | Animate the customize sunglasses-tab camera zoom | |
| Low | Subtle monetization path | Idea stage only. |
| Low | Controller vibration strength *(pre-ship 07-19)* | |

## Playtest owed

Stuff that shipped and still needs your eyes on **production**
(https://cart-rave.wyabro.workers.dev — hard-refresh first).  
Console: `npm run dashboard` → playtest console. Mark closed by rewriting Notes to
`Wyatt playtest PASS — …` (drop the `Owed:` line).

Finished and removed from this list: **FV-FRIENDS-1**, **FV-REMATCH-1** (PASS 08-02).

| Pri | Item | Notes |
|-----|------|-------|
| High | ROUND-WEDGE-1 — no podium flicker storm | **Owed: Wyatt playtest — ROUND-WEDGE-1 — results screen stays put; no 25×/s flicker.**<br>1. Hard-refresh prod as **host** (quickplay is fine). Optional: `?diag=1`.<br>2. Play a normal timed round all the way to 0:00. **Pass if:** one clean results/podium screen, no flashing back into the match.<br>3. Host-hide check (Phase A): mid-round, hide the tab ~30–60s, come back, finish the timer. **Pass if:** round still ends cleanly on podium (not stuck running forever).<br>4. If you ever *do* see a reject/rollback: at most a brief flash, then stable — not a rapid flicker. Rematch after that still ends fine next round.<br>5. Non-host (or second client) on a clean end: normal podium, no softlock.<br>**cap-217 stays open until you say PASS.** |
| High | SUNDIAL-PT-1 — Sundial art (all waves) | **Owed: Wyatt playtest — SUNDIAL-PT-1 — Sundial Station looks and plays right.**<br>1. Open Sundial on prod (hard-refresh). Walk the deck a bit.<br>2. Check the hologram reads like a **projection**, not a floating prop stuck in mid-air.<br>3. Drive into the gnomon/spindle — you should **hit** it (collider), not ghost through.<br>4. Get a KO if you can — flare should pop without wrecking the whole palette.<br>5. If you have the weak Intel box: try Low quality and decide if the reduced holo is still worth it.<br>6. Quick sweep while you play: cart shadows stay flat when you turn; dust/sun lobe; light shafts feel alive; dial, gate beacons, ship glows, turbines still there. Phone is a bonus pass.<br>Handover detail: [art-pass-sundial-handover.md](./art-pass-sundial-handover.md). |
| High | STORE-PT-1 — Storerooms art | **Owed: Wyatt playtest — STORE-PT-1 — suction edge + shelves feel real.**<br>1. Load The Storerooms on prod.<br>2. Drive to the suction/void lips. **Pass if:** the edge feels like a real drop you commit past — not a flat game-marker painted on the floor.<br>3. Look at the racking/shelves. **Pass if:** they read as used steel, not just a darker wall slab.<br>Still open later (not this check): density retune, **SHELF-RAIL-1**. Audit: [art-audit-storerooms.md](./art-audit-storerooms.md). |
| High | RESULTS-ACT-1 — PLAY AGAIN in a short window | **Owed: Wyatt playtest — RESULTS-ACT-1 — PLAY AGAIN visible without scrolling.**<br>1. Open prod Solo.<br>2. Open the browser console and run: `CartClashDev.run("scores 7 4 2 9")` then `CartClashDev.run("rewind 800")` (forces podium in ~2s).<br>3. Shrink the window to roughly **900×390** (or max-height under 640).<br>4. **Pass if:** you can see and click **PLAY AGAIN** without scrolling the results panel. |
| High | FV-HUD-1 — in-match HUD feel | **Owed: Wyatt playtest — FV-HUD-1 — Fight Night HUD in a real match.**<br>1. Start a Solo (or short) round on prod.<br>2. Play a minute: watch timer, nameplates, kill feed, boost bar, and open pause once.<br>3. **Pass if:** it all feels Fight Night and nothing is clipped, missing, or broken. |
| High | FV-RESULTS-1 — results podium feel | **Owed: Wyatt playtest — FV-RESULTS-1 — results screen feels finished.**<br>1. Finish the same round (or force podium like RESULTS-ACT-1).<br>2. Watch the count-up / receipt cadence.<br>3. **Pass if:** victory or defeat treatment is clear, and SPILLS / CHALLENGE lines look intentional (not placeholder junk). |
| High | FV-WILT-1 — confetti / wilt + winner slot | **Owed: Wyatt playtest — FV-WILT-1 — win/lose FX + winnerSlotIndex on MP.**<br>1. Two clients, same friends or quickplay room on prod.<br>2. Play until podium; one machine should be the loser.<br>3. Note: any confetti on win / wilt (or similar) on loss? Yes / no is enough.<br>4. On the losing client console: `GameState.getRoundState().winnerSlotIndex` — is it a **number** or **null**?<br>One honest observation closes this card (parked observation, not a polish pass). |
| High | UNLOCK-PT-1 — progression gates | **Owed: Wyatt playtest — UNLOCK-PT-1 — locked arenas make sense.**<br>1. Open prod with gates ON: add `?devUnlocks=off` then **hard refresh** (Vite unlocks everything by default without this).<br>2. Fresh / cleared save path: **Sundial Station** should be playable; Storerooms + Cart Rave locked with hints that name the right arena.<br>3. If you already have progress: you keep what you had — no wipe surprise.<br>4. **Pass if:** locked vs unlocked matches that story. |
| High | PIT-PT-1 — Cart Rave pit rim | **Owed: Wyatt playtest — PIT-PT-1 — rim stops the basket, not swallows it.**<br>1. Open **Cart Rave** on prod.<br>2. Ram the pit rim **hard in several places** around the ring (one lucky spot is not enough).<br>3. **Pass if:** the basket stops **at** the wall, not buried in it; falls still feel normal.<br>4. Glance down the shaft on a fall — depth should still read (shorter than the old absurd tunnel). |
| ✅ | SPAWN-PT-1 — spawn decks | **CLOSED 08-03 — Wyatt accepted centring.** Inset shipped in `e64f1a3` (decks + spawn ring moved together 0.75 m); centring was already correct. Nothing fixed in PLAYTEST-BATCH-0803. Residual platform-leg colliders stay on **SPAWN-SUNDIAL-1** (own card). |
| ✅ | CAM-PT-1 — solo opening camera | **CLOSED 08-03 — Wyatt PASS** (orbit feel + quit/cancel clean). Residual dead 2s hold window → **CAM-READY-1** (GET READY pulse during pre-roll). Do not reopen CAM-PT-1. |
| High | SHADOW-TILT-1 — cart ground shadows | **Owed: Wyatt playtest — SHADOW-TILT-1 — blob shadow stays flat when you turn.**<br>1. Any arena on prod. Drive and spin the cart in place / through yaw turns.<br>2. **Pass if:** the ground blob stays on the floor and roughly the same size (does not tip up edge-on and vanish when you face world X). |
| Medium | FV-BOOT-1 — cold boot splash | **Owed: Wyatt playtest — FV-BOOT-1 — first paint splash is real.** + retest after batch lever (W0.2 measure → Wave 7).<br>1. Hard-refresh prod (`?diag=1` optional), cache cleared cold boot.<br>2. Watch the **first** paint and how the splash exits — not a later remount.<br>3. **Pass if:** boot splash feels intentional and exits cleanly near the floor; F8 shows boot segment marks. |
| Medium | FV-LOAD-1 — mode-entry loading per arena | **Owed retest after PLAYTEST-BATCH-0803 Waves 1–3.** Split: **1b** entry freeze + wrongful quality step-down; **1a** loading-screen sizing + Fight Night restyle.<br>1. From the menu, enter **Cart Rave** twice: no multi-second freeze; no `qualityStepDown` during entry; tier survives.<br>2. Enter **Classic / Storerooms / Sundial** once each — loading decor fills the stage at 1440p+ and reads Fight Night; phone unchanged.<br>3. F8: `play-shader-start/end` span ~0 after warm juice path. |
| Medium | UNLOCK-TOAST-1 — locked-arena toast clears bottom bar | **Owed: Wyatt playtest — UNLOCK-TOAST-1 — main-menu arena toast only.** (Not UNLOCK-PT-1 progression order.)<br>1. Main menu, gates on: click a locked arena. **Pass if:** toast sits above the bottom hint bar.<br>2. Open Customize, trigger a locked-color toast. **Pass if:** that toast sits in the original lower spot (no stuck lifted class). |
| Medium | STORE-DECK-1 — Storerooms spawn-platform textures | **Owed: Wyatt playtest — STORE-DECK-1 — decks read as worn plate.** (Not STORE-PT-1 suction/racking.) Art-audit item 6.<br>1. Load The Storerooms. Look at spawn booth decks. **Pass if:** worn plate + safety stripe + bay letter; not flat grey slabs.<br>2. Enter play — no new entry hitch (shader span unchanged vs pre-card). |
| High | LOAD-POSTER-1 — loading screens as Fight Night posters | **Owed: Wyatt playtest — LOAD-POSTER-1 — the art carries the frame.** Follow-up after FV-LOAD-1a's sizing pass still read too small. Desktop res, all three arenas.<br>1. Enter **Cart Rave** — club floor: mirror ball, beam fans, crowd along the floor line. Title is a two-line poster lockup, not a corner label.<br>2. Enter **The Storerooms** — liminal aisle, one chair mid-aisle, boxes foreground. **Fail if it reads rave-y** (neon/glow); this arena stays liminal.<br>3. Enter **Sundial Station** — golden hour, **not dark**. Big sun behind the title, gnomon blade off to the left.<br>4. **Pass if:** no dead field at your native resolution and each screen reads as the same design family as the boot splash.<br>5. Watch the entry itself — the scene animates while shaders compile, so any new stutter at mode-entry is a fail (report with an F8). |
| Medium | CAM-READY-1 — GET READY pulse during solo hold | **Owed: Wyatt playtest — CAM-READY-1 — 2s hold is not dead air.** Follow-up to closed CAM-PT-1.<br>1. Solo on prod. During the ~2s pre-3-2-1 orbit, **GET READY** should pulse.<br>2. Hands off cleanly into 3-2-1 digits (no double stamp mess).<br>3. Quit mid-hold — no stuck HUD. MP countdown unchanged. |
| Low | AQ-RING-CLEAR-1 — autoQuality clear sample ring on every window eval | **Reserve only** if Wave 2 entry grace still demotes on retest. Comment in autoQuality.js already notes the ring can poison up to 3 windows. Own commit if needed; not in main batch path. |
| Medium | FV-SILVER-1 — silver sunglasses | **Owed: Wyatt playtest — FV-SILVER-1 — silver finish is readable.**<br>1. Customize → pick the **silver** sunglasses finish.<br>2. **Pass if:** border / glow / pip are readable enough to tell selected vs not. |
| Medium | SOLO-PT-1 — solo defaults to medium | **Owed: Wyatt playtest — SOLO-PT-1 — first solo feels like a fight.**<br>1. Fresh solo with no remembered difficulty (or clear that setting if you know how).<br>2. Play a bit. **Pass if:** bots feel like medium (a real fight), not a free warm-up. If it’s too hard, say so — fix is the default, not a whole retune. |
| Low | HOST-TOAST-1 — weak-host toast | **Owed: Wyatt playtest — HOST-TOAST-1 — toast once, not spam.** Optional.<br>1. Only if you can host with a weak join-time score (&lt;50).<br>2. **Pass if:** toast shows **once** per hostship, not every frame / join. |
| Low | CC-PT-1 — Command Center look | **Owed: Wyatt playtest — CC-PT-1 — Command Center tokens look right.**<br>1. Run `npm run cc` (or open the dashboard HTML it builds).<br>2. Skim the main surfaces: no wrong/invisible colours, no coloured side-stripes on cards, labels sentence case (micro-caps only on section heads), nav icons match the intended look.<br>3. Optional: `npm run sheet` or `states` once — montage should share the same tokens. |

## UI / UX

| Pri | Item | Notes |
|-----|------|-------|
| High | PRE-PODIUM-1 — confetti / wilt + win-lose callout missing `[pre-ship]` | Victory confetti and defeat wilt missing in **MP and solo** pre-podium. Also missing **“{playerName} wins / loses”** (or equivalent) at round end before the podium screen. Fight-night handover already flagged MP confetti (parked 07-23); this card re-opens both modes + the name callout. Art look of wilt still under Wilting-groceries `[SHIP-1 E2]`. Observation eye for current MP behavior is **FV-WILT-1** in Playtest owed. |
| High | ONBOARD-SLIDES-1 — how-to-play as click-through visual slides `[pre-ship]` | Replace wall-of-text “how to play” with a **slideshow**: card-by-card slides the player can click through, each with GIF / short animation / picture so rules land fast. Supersedes wall-of-text path of **ONBOARD-1**; keep Solo-as-tutorial stance. Needs asset list (slide topics + art) before full polish. |
| High | MENU-LOCK-HINT-1 — locked-level menu explains why `[pre-ship]` | New main menu has no affordance when a level is locked (why locked / how to unlock). Add tooltip, disabled-state copy, or tap-to-explain — must match Fight Night chrome. |
| High | RESULTS-1 — results screen layout redesign `[SHIP-1 E1]` | |
| Medium | Controller menu navigation polish *(pre-ship 07-19)* | Modal-scoping shipped 07-20; remaining = polish + pad-in-hand validation. |
| Medium | UI-FRAME-1 — premium frame/panel styling pass `[SHIP-1 E1]` | |
| Medium | ESC scoring panel refresh `[SHIP-1 E1]` | |
| Low | Main-menu SFX slider `[SHIP-1 E3]` | |
| Medium | ONBOARD-1 — first-run controls card `[SHIP-1 E4]` | Fold into **ONBOARD-SLIDES-1** (visual cards). Residual only if a one-shot controls reminder is still needed after slides. Solo remains the live tutorial. |
| High | FIGHT-VERIFY-1 — owed fight-night verification | Agent half DONE 08-01. **Wyatt half = still-open playtest cards** (RESULTS-ACT-1 · FV-HUD-1 · FV-RESULTS-1 · FV-WILT-1 · FV-BOOT-1 · FV-LOAD-1 · FV-SILVER-1). Friends/rematch already PASS. Parent is docs only — seed lives on those rows. Handover: [fight-night-ui-handover.md:252+](./fight-night-ui-handover.md). |
| Medium | LOAD-SCALE-1 — the mode-entry loading screen is ~99% empty space above ~1000px wide | **Found 07-31 by `npm run loadshots`** (FIGHT-VERIFY-1 Phase C) — the first time any of the three arena themes had been rendered by this toolkit. The centre-stage decor is hard-capped and never grows: `loadingScreen.css:157-162` `.cr-load__visual { width: clamp(88px, 22vw, 128px); height: clamp(88px, 22vw, 128px) }`, with backrooms at `clamp(96px, 24vw, 140px)` × `clamp(72px, 18vw, 96px)` (`:349-352`) and zanzibar's `.cr-load__seaside` at `clamp(110px, 26vw, 160px)` × `clamp(72px, 18vw, 96px)` (`:378-383`). At 390×844 that is 88px in a 390px frame — proportionate, and the design reads exactly as intended. At 1920×1080 the same motif is at most 128×128px inside a centre row of roughly 1920×820, i.e. **under 1% of the stage**, leaving the vinyl / boxes+chair / sun+deck floating as a postage stamp in an otherwise empty gradient with the title marooned in the top-left corner. Nothing is clipped or overflowing — the `overlay fits the viewport` check is green on all 8 cells — which is precisely why a DOM-geometry sign-off could never have caught it. Belongs in the UI-SCALE-1 pass rather than as a standalone fix. Evidence: `.diag-captures/loadshots/mode-{classicRecord,backrooms,zanzibar}-1920x1080-b-mid.png` versus `mode-classicRecord-390x844-b-mid.png`. |
| Low | STATES-DEAD-1 — interactive-state CSS for elements that no longer render | **Found 07-31 by `npm run states`** (FIGHT-VERIFY-1 Phase B) via its reachability family, which enumerates every `:hover`/`:active`/`:focus-visible` rule from the live CSSOM and asserts each one matches a live element on some visited screen. Seven subjects match nothing, on any of the 11 screens the tool sweeps (menu · customize + its sunglasses and patterns tabs · settings · challenges · how-to · in-match HUD · pause · results podium · touch in-match): **(1) `.cr-touch-btn`** (`cart-rave-menu.css:182`, `:active`) and **(2) `.cr-kbm-toast-close`** (`:178`, `:hover`/`:active`) — both class names appear in **zero `.js`/`.ts`/`.html` files repo-wide**; the CSS is all that is left of them. **(3) `.cr-level-btn`** (`:529` `:hover:not(.cr-level-btn--disabled)`, `:2400` inside the designed focus ring, `:3289` in the reduced-motion block) — the arena radiogroup at `index.html:703` carries the `hidden` attribute and its own comment calls it a "hidden radiogroup: arena data source"; the visible control is the `.cr-arena-page` pager. That also means the nine-selector designed focus ring at `:2398-2409` is an **eight**-selector ring in practice. **(4-6) `a`, `select`, `[role="button"]`** — three of the five element types listed in the unscoped fallback ring at `src/ui/loadingScreen.css:577`; no `<select>` and no `role="button"` element exists anywhere, and the only `<a>` elements created are transient download links (`main.js:5604`, `postFxDebug.js:190/241`). Harmless defensive CSS, but it means that rule is doing two-fifths of the work it looks like it does. All seven are held in `DECLARED_UNREACHABLE` in `tools/states.mjs` with these reasons, printed on stdout every run and in the montage banner — **and if any of them ever starts matching again the run says so out loud**, so declaring them does not hide a future regression. |
| Low | TOUCH-HOVER-1 — no `@media (hover: hover)` guard anywhere, so hover states latch on touch | **Measured 07-31 by `npm run states`** (FIGHT-VERIFY-1 Phase B) on a `hasTouch + isMobile` 390×844 client. There are **zero** occurrences of the `(hover:` media feature in `src/**/*.css` — the only three hover-capability checks in the repo live in JS (`touchControls.js:580`, `rotatePrompt.js:124`, `device.js:36`) — so every `:hover` rule applies on a coarse-pointer device, where a tap latches it until the user taps elsewhere. Reported as **one aggregate advisory row**, not one failure per selector: it is one missing media query, and 30-odd reds would drown the tally. Measured with `CSS.forcePseudoState`, **not** synthetic mouse input — Chromium under mobile emulation routes `mouse.move()` through the touch pipeline and produces no `:hover` at all, which made an earlier mouse-driven pass under-report this (1/6) before the method was corrected. Result on two consecutive runs: **2 of the 5** (run B: 2 of 4) hover rules whose subject is actually on screen at 390×844 still repaint — `.cr-reroll` and `.cr-plate .cr-plate-btn`. The denominator is small because the phone menu shows fewer controls, and the non-repainting remainder are exactly the dead ones filed as **MENU-CMD-FEEL-1** / **MENU-NAME-HOVER-1** — i.e. **every hover rule that has a live delta at all does apply on touch**. Low because the visual result is mild (a re-roll button and a plate button staying lit after a tap), but it belongs to the same pass as UI-SCALE-1's ≤768 reflow. |
| High | UI-SCALE-1 — responsive root-scale migration | Two passes per [responsive-scale-migration.md](./responsive-scale-migration.md): fluid root + rem clamps; media queries stay px. **Pass 1 also does ≤768 reflow — phone = fewer elements.** SHEET-1 unblocked (tool exists). Sequential single-owner. Safety: ≥1920 pixel-identical pre/post. |

## Tech Debt

Jam-era structure that still works but accrues cost. Prefer seams after multiplayer is proven.
Priorities below are post-gate unless Wyatt pulls them forward.

| Pri | ID | Item | Notes |
|-----|----|------|-------|
| ✅ | BRIEF-DIGEST-1 | Briefing freshness digest ignores the template | **CLOSED 08-03** — riefingSourceDigest = sha8(templateFingerprint + body); enderBriefingMd embeds riefingSourceDigest(statusMd) (not sha8(body)). Shared constants for title/boilerplate/read-order/before-touch. Tests in 	ests/projectHealth.test.js (not a new briefing.test.js). |
| ✅ | SKILLSYNC-PRUNE-1 | `skills-sync` never removed orphaned skills from the mirror | **CLOSED 08-02** — fixed while rewriting the file for multi-runtime fan-out, where the bug got 7× worse. New pure `planPrune(src, dest)` diffs destination against source; `planAll()` reports orphans **only for owned destinations**. Pruning is deliberately asymmetric: the repo mirror is created and owned here so orphans are deleted, but user-level dirs are shared with other installers (`~/.cursor/skills` holds 13 skills this repo never placed) and are add/update only, forever. Covered by 5 new tests including the "never reports orphans for a shared destination" invariant. |
| ✅ | SHADOW-TILT-1 | Cart blob shadows tilt out of the floor as the cart turns | **Code CLOSED 08-02** (`b36be5c` + orientation tests). **Human check still open** — see **Playtest owed → SHADOW-TILT-1**. |
| Medium | SHADOW-ORDER-1 | Storerooms static shadows may be tested against the wrong arena | **Filed 08-02**, same root cause as the build-order bug fixed for Sundial in `17fc46c`. `main.js:2458` calls `setContactShadowHazards` **after** the level builder returns, so any `createStaticContactShadowCluster` call made during construction is tested against the *previous* arena's hazards, or the circular fallback (`r > CONFIG.record.radius`) on a cold load. Sundial now passes its own `hazards` override; `backroomsSupermarket.js:1884` (furniture pile) and `:3229` (booth shadows) still do not. The pile sits near the origin and almost certainly survives either way, but the booth placements are outboard and were never verified. Check by probing the live scene for `StaticContactShadows` child counts on a cold load vs an arena switch — the Sundial case read 8 of 24 and looked exactly like "the feature is subtle". |
| Medium | SHIP-1 | V2 shipping checklist + final QA doc | **Created 07-20** — [SHIP-1.md](./SHIP-1.md), living doc; row stays as pointer until ship. |
| Medium | MAIN-1 | Carve `main.js` composition seam | Prerequisite for BUNDLE-1. |
| Medium | STORE-1 | Collapse `gameState` facade dual import | |
| Medium | DIR-1 | Directive modifiers without mutating `CONFIG` | |
| Medium | TRUST-1 | Worker validates host-asserted outcomes | Prerequisite for trusted leaderboard. Builds on SRV-TEST-1 helpers. `[SHIP-1 D1]` *(was also an Engineering row — deduped 08-01)* |
| Low | BUNDLE-1 | Menu/game code-split | Blocked on MAIN-1 (D-PERF-3). |
| Low | GLTF-1 | Drop legacy cart GLTF layout path | |
| Low | DUAL-1 | Delete leftover dual-era paths | |
| Low | TS-1 | TypeScript on hot paths / TS 7 | Stay on TS 6.x for the gate. |
| Low | TOOL-1 | Tooling residue | |
| Low | ARCH-DRIFT-1 | `archMap.mjs` curated prose has no staleness gate | **Found 08-01.** `arch:check` digest-gates only the *generated* JSON — nothing validates the hand-written seed in `tools/lib/archMap.mjs` against real code, so it drifted silently (two claims were outright false until the 08-01 pass). Every hardcoded line ref is stale: `main.js` `314→342`, `4748→5172`, `2149→2301`; `simulation.js` `2717→2889`; `party/index.ts` `801→1056`. Also "~5.6k lines"→5773, "~84 inner fns"→81, "~40 impls"→49/50. Proposal: replace line refs with grep-able symbol anchors + a test that fails when an anchor stops resolving. |
| Low | Vite 500 kB chunk-size hint | Cosmetic. |
| Low | BRAND-1 | Brand / domain cutover ceremony | Frozen — [brand.md](../brand.md). |
| Low | RESULTS-GLOW-1 | `.results-defeat .results-title { --title-glow }` never applies | **Note for later (cosmetic, pre-existing).** `main.js` sets that property **inline**, so no stylesheet rule can outrank it. Defeat still reads because the panel filter desaturates everything. Do **not** reach for `!important` unless a real look pass re-owns title styling. Not a ship blocker. |
| Low | AUDIO-MASTER-1 | `_masterVol` is write-only dead state | **Filed 08-02** during MENU-MUSIC-VOL-1. `audioManager.js:14` initialises `_masterVol = 0.575` and `restoreVolumeState` (`:233`) clamps and re-assigns it — but **nothing ever reads it**, so the "master" volume the boot path carefully restores from localStorage has no effect on any bus. Music and SFX each carry their own category volume (`_musicVol` / `_sfxVol`) and `applyAllVolumes` deliberately pins `Howler.volume(1)`, so there is no master stage left for it to feed. Either wire it up as a real third stage (product decision — it would rescale every existing user's audio) or delete it and drop `master` from the restore payload. **Do not** "fix" it by multiplying it into the Howler writes without a look at MENU-MUSIC-VOL-1's clamp: the store domain is 0..1.15 and Howler's ceiling is 1.0, so an extra 0.575 factor would silently re-scale the whole mix. Not a ship blocker; zero current behaviour. |
| Low | CC-ESC-1 | Two `esc()` implementations with different escaping | `montage.mjs:19` escapes `[&<>"]`; `ccStyle.mjs:92` escapes `[&<>"']` — the apostrophe is the delta. Consolidating changes the output of every string the capture sheets render, so it needs **its own card and its own test**, not a drive-by during a styling pass. The import path already exists: `montage.mjs` took `ROOT_TOKENS` from `ccStyle.mjs` on 08-01, so only the behaviour difference blocks this. |

### Explicitly *not* tech debt (do not “modernize” these)

| Topic | Why leave it |
|-------|----------------|
| Host-**authoritative** Rapier (clients may predict) | Architecture invariant — clients step the same world locally for feel; the host is sole authority and the server never simulates. [AGENTS.md](../../AGENTS.md). |
| Zustand + KO event reactors | Current and coherent. |
| partyserver + WebRTC P2P split | Control plane vs gameplay plane is correct. |
| Big `config.js` knob table | Fine if knobs stay centralized; DIR-1 stops mid-round mutation. |

## Future Ideas (post-launch)

- WebGPU compute shaders for targeted VFX — after mobile perf; no physics rewrite.
- Economy/XP progression beyond lifetime unlocks — only if reopened deliberately.
- Domain + full rebrand cutover (BRAND-1).
- MAIN-1 → BUNDLE-1 after V2.
- DIR-1 runtime modifier stack if Living Store grows mutators.
- GLTF-1 legacy layout deletion after cartrave4-only sign-off.
